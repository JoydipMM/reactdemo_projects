https://www.youtube.com/watch?v=4nVoLX2taFg&list=PL6QREj8te1P54rZQx5AWWtFyf1hlznFjL
https://www.figma.com/design/m3jtoedc0WWAoG6kiJZtxz/Subscription-Management-App?node-id=0-1&p=f
https://jsmastery.com/video-kit/906b6d70-b862-4257-85dd-56b3ba76db59

Tailwind CSS IntelliSense with Hex Colors
Fullstack React/React Native snippets
ES7+ React/Redux/React-Native snippets



current node: v24.15.0

nvm install 20.20.0
nvm use 20.20.0


npx create-expo-app@latest --template default@sdk-54 ./
** node version required 20


npx create-expo-app my-app
cd my-app
npx eas-cli@latest login
npx eas-cli@latest init


npx expo start

--- remove defalt demo design ---
npm run reset-project [remove demo files]
Do you want to move existing files to /app-example instead of deleting them? (Y/n): n
npx expo start

Tailwind CSS : https://www.nativewind.dev/v5
npx expo install nativewind@preview react-native-css react-native-reanimated react-native-safe-area-context
npx expo install --dev tailwindcss @tailwindcss/postcss postcss

in root folder > create postcss.config.mjs
in postcss.config.mjs
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

in root create global.css
in global.css
@import "tailwindcss/theme.css" layer(theme);
@import "tailwindcss/preflight.css" layer(base);
@import "tailwindcss/utilities.css";
 
@import "nativewind/theme";

Create or modify your metro.config.js
npx expo customize metro.config.js
const { getDefaultConfig } = require("expo/metro-config");
const { withNativewind } = require("nativewind/metro");
 
/** @type {import('expo/metro-config').MetroConfig} */
const config = getDefaultConfig(__dirname);
 
module.exports = withNativewind(config);

--- import global.css in main .tsx file
app > _layout.tsx
import "@/global.css";

-- package.json add the json object after devdependecy object
"overrides": {
    "lightningcss": "1.30.1"
  }

--- create nativewind-env.d.ts in root folder
/// <reference types="react-native-css/types" />

// NOTE: This file should not be edited and should be committed with your source code. It is generated by react-native-css. If you need to move or disable this file, please see the documentation.

app\index.tsx
import { Text, View } from "react-native";
import "@/global.css";

export default function App() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-xl font-bold text-blue-500">
        Welcome to Nativewind!
      </Text>
    </View>
  );
}


npx expo start --clear [clear cache]



Routing And navigation
-------------------------------------------
create Onboarding page 
app\onboarding.tsx
import React from 'react';
import { View, Text } from 'react-native';

export default function Onboarding() {
  return (
    <View>
      <Text>onboarding</Text>
     </View>
  );
}

app\index.tsx
import { Link } from "expo-router"

<Link href="/onboarding">Onboarding</Link>


Layout
---------------------------------
if we go in onboarding page. "onboarding" title show in header
but if we go in signup/signin page the title will be show like "(auth)/signin" or "(auth)/signup"
because expo use "stack navigation" by default and each layout use it's own header
So we will hide that header by create "_layout.tsx" inside (auth) folder
_layout.tsx:
import "@/global.css";
import { Stack } from "expo-router";

export default function RootLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}

if we can to hide header from root layout then add "screenOptions={{ headerShown: false }}" with <stack/> 


Tabs / bottom navigation
--------------------------------------------------
app\(tabs) -> create "subscription.tsx", "insight.tsx" and "settings.tsx", we can also move "index.tsx" inside (tab) group folder. but the "index.tsx" will work as before.

create "_layout.tsx"
in "_layout.tsx"
import { Tabs } from "expo-router";
import React from "react";

const TabsLayout = () => (
  <Tabs screenOptions={{ headerShown: false }}>
    <Tabs.Screen name="index" options={{ title: "Home" }} />
    <Tabs.Screen name="subscriptions" options={{ title: "Subscriptions" }} />
    <Tabs.Screen name="insights" options={{ title: "Insights" }} />
    <Tabs.Screen name="settings" options={{ title: "Settings" }} />
  </Tabs>
);

export default TabsLayout;

Above code will create bottom tab group in app with "Home", "Subscriptions", "Insights" and "Settings" link 


Dynamic Routes
--------------------------------------------------
We make a subscription detail page. for that we create "subscription" folder inside root folder.
inside "subscription" folder we create two .tsx file
1 >>> [id].tsx
2 >>> spotify.tsx

in [id].tsx : --------------------------
import { Link, useLocalSearchParams } from "expo-router";
import React from "react";
import { Text, View } from "react-native";

const SubscriptionDetails = () => {
  const { id } = useLocalSearchParams<{ id: string }>();
  return (
    <View>
      <Text>subscription/[SubscriptionDetails] - of {id}</Text>
      <Link href="/" className="py-2 px-5 bg-primary text-white m-2">
        Go back
      </Link>
    </View>
  );
};

export default SubscriptionDetails;


spotify.tsx -------------------------
import { Link } from "expo-router";
import React from "react";
import { Text, View } from "react-native";

const Spotify = () => {
  return (
    <View>
      <Text>Spotify</Text>
      <Link href="/" className="py-2 px-5 bg-primary text-white m-2">
        Go back
      </Link>
    </View>
  );
};

export default Spotify;

app\(tabs)\index.tsx
add links:
<Link
        href="/subscription/spotify"
        className="py-2 px-5 bg-primary text-white m-2"
      >
        Spotify Subscription
      </Link>
      <Link
        href={{
          pathname: "/subscription/[id]",
          params: { id: "anitigravity" },
        }}
        className="py-2 px-5 bg-primary text-white m-2"
      >
        anitigravity Subscription
      </Link>



If we put "subscription" folder inside "(tabs)" folder, then in bottom tabs section extra two links will show, 
1 >>> subcription[id]
2 >>> subscription/spotify

but we don't want to show this two links in tabs section. To hide this two link we will update "_layout.tsx" file of "(tabs)" folder
<Tabs.Screen name="subscriptions/[id]" options={{ href: null }} />
<Tabs.Screen name="subscriptions/spotify" options={{ href: null }} /> 

above two line will hide the two links


Use Icons images with constant
----------------------------------------
Create app\constants\icons.ts file

import activity from "@/assets/icons/activity.png";
import add from "@/assets/icons/add.png";
import adobe from "@/assets/icons/adobe.png";
import back from "@/assets/icons/back.png";
import canva from "@/assets/icons/canva.png";
import claude from "@/assets/icons/claude.png";
import dropbox from "@/assets/icons/dropbox.png";
import figma from "@/assets/icons/figma.png";
import github from "@/assets/icons/github.png";
import home from "@/assets/icons/home.png";
import medium from "@/assets/icons/medium.png";
import menu from "@/assets/icons/menu.png";
import notion from "@/assets/icons/notion.png";
import openai from "@/assets/icons/openai.png";
import plus from "@/assets/icons/plus.png";
import setting from "@/assets/icons/setting.png";
import spotify from "@/assets/icons/spotify.png";
import wallet from "@/assets/icons/wallet.png";

export const icons = {
  home,
  wallet,
  setting,
  activity,
  add,
  back,
  menu,
  plus,
  notion,
  dropbox,
  openai,
  adobe,
  medium,
  figma,
  spotify,
  github,
  claude,
  canva,
} as const;

export type IconKey = keyof typeof icons;

Now to handle image import we create "image.d.ts" declaration file in project root folder
in "image.d.ts"
declare module "*.png" {
  const value: any;
  export default value;
}
declare module "*.jpg" {
  const value: any;
  export default value;
}
declare module "*.jpeg" {
  const value: any;
  export default value;
}
declare module "*.svg" {
  const value: any;
  export default value;
}
declare module "*.gif" {
  const value: any;
  export default value;
}

this line of codes will help to read the images

also create "type.d.ts" in project root folder. This declare the type of image source type
in "type.d.ts"
import type { ImageSourcePropType } from "react-native";

declare global {
  interface TabIconProps {
    focused: boolean;
    icon: ImageSourcePropType;
  }
interface AppTab {
    name: string;
    title: string;
    icon: ImageSourcePropType;
  }
}

export { }; 


Create data for tabs "constants/data.ts"
import { icons } from "./icons";

export const tabs: AppTab[] = [
  { name: "index", title: "Home", icon: icons.home },
  { name: "subscriptions", title: "Subscriptions", icon: icons.wallet },
  {
    name: "subscriptions/spotify",
    title: "spotify",
    icon: icons.wallet,
    href: null,
  },
  {
    name: "subscriptions/[id]",
    title: "subscription[id]",
    icon: icons.wallet,
    href: null,
  },
  { name: "insights", title: "Insights", icon: icons.activity },
  { name: "settings", title: "Settings", icon: icons.setting },
];


Style Bottom Tab Bar
----------------------------------------
install npm i clsx
this help multiple class join together

app\(tabs)\_layout.tsx here we update our tabs code:
import clsx from "clsx";
import { Tabs } from "expo-router";
import React from "react";
import { Image, View } from "react-native";
import { tabs } from "../constants/data";

const TabsLayout = () => {
  const TabIcon = ({ focused, icon }: TabIconProps) => {
    return (
      <View className="tabs-icons">
        <View className={clsx("tabs-pill", focused && "tabs-active")}>
          <Image source={icon} className="tabs-glyph" />
        </View>
      </View>
    );
  };

  return (
    <Tabs screenOptions={{ headerShown: false }}>
      {tabs.map((tab) => (
        <Tabs.Screen
          key={tab.name}
          name={tab.name}
          options={{
            title: tab.title,
            tabBarIcon: ({ focused }) => (
              <TabIcon focused={focused} icon={tab.icon} />
            ),
            href: tab.href,
          }}
        />
      ))}
      {/* <Tabs.Screen name="index" options={{ title: "Home" }} />
            <Tabs.Screen name="subscriptions" options={{ title: "Subscriptions" }} />
            <Tabs.Screen name="subscriptions/[id]" options={{ href: null }} />
            <Tabs.Screen name="subscriptions/spotify" options={{ href: null }} />
            <Tabs.Screen name="insights" options={{ title: "Insights" }} />
            <Tabs.Screen name="settings" options={{ title: "Settings" }} /> */}
    </Tabs>
  );
};

export default TabsLayout;





Safearea view
----------------------------------------
npm i react-native-safe-area-context








