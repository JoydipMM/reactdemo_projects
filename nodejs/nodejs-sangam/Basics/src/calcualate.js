function sum(a, b) {
  return a + b;
}

function minus(a, b) {
  return a - b;
}

module.exports = {
  sum,
  minus,
}; // exporting sum and minus from this module so that it can be used in other files.