http://localhost:3000/events/unnat-bharat-abhiyan-2025/register


react form library: https://formik.org/


utils\firebase.js
paste the firebase SDK setup and configuration
// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  ...
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);

in firebase console: [project/flimflixgpt/authentication/providers]
Authentication > Sign-in method > add provider > choose provider
here I choose > Email/Password > Email/Password check Enable

npm install firebase
npm install -g firebase-tools
firebase login (in console) follow step if not loggedin in firebase
firebase init
-- Are you ready to proceed? yes
-- Hosting : Static Web App
npm run build
firebase deploy

{
  "hosting": {
    "public": "dist", // this is for setup react app with vite 
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ]
  }
}
npm run build
firebase deploy



signup user:
firebase.js
import { getAuth } from "firebase/auth";

Login.jsx:
import { createUserWithEmailAndPassword, signInWithEmailAndPassword } from "firebase/auth";


Redux setup:
npm install -D @reduxjs/toolkit
npm install react-redux

